# Local Development - SQLite (use this for testing locally)
DATABASE_URL=sqlite:///./fund_tracker.db

# Production - PostgreSQL (DO NOT commit this URL to Git!)
# When deploying to Render, you'll get a PostgreSQL URL like:
# DATABASE_URL=postgresql://user:password@hostname.render.com/database_name
# Add that URL in Render's Environment Variables dashboard, NOT here

SECRET_KEY=supersecretkeychangeit
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
